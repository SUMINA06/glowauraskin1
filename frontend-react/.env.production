# Production (Railway) backend service URL, reachable via Railway's
# private networking. Used by vite.config.js to configure the dev/preview
# server proxy target for the /api and /uploads routes.
VITE_API_PROXY_TARGET=http://glowauraskin1.railway.internal:8080
